[project]
name = "broken-tool"
version = "0.0.1"
description = "Deliberately broken fixture used by the validator tests."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "python-dotenv>=1.0",
]
