[project]
name = "reference-tool"
version = "0.1.0"
description = "Square-root reference fixture for check #12 testing."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.32",
  "python-dotenv>=1.0",
]
