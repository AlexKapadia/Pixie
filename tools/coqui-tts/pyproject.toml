[project]
name = "coqui-tts"
version = "0.1.0"
description = "Offline text-to-speech using pyttsx3 (SAPI on Windows)."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "python-dotenv>=1.0",
  "pyttsx3>=2.91",
  "numpy>=1.26",
]
