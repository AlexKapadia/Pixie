# Open-Meteo Forecast

Fetch a multi-day forecast for any latitude/longitude using the free Open-Meteo API (no API key).

## Install
```
cd tools/open-meteo-forecast
uv sync
```

## Test
```
uv run pixie validate open-meteo-forecast --summary
```
