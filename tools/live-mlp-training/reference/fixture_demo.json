{
  "name": "Demo dataset, 5 epochs",
  "source": "600-row synthetic XOR-ish dataset, seed 42",
  "inputs": {
    "target_column": "target",
    "hidden_sizes": "16,8",
    "epochs": 5,
    "learning_rate": 0.05,
    "batch_size": 32,
    "seed": 42
  },
  "expected_outputs": {
    "final_metrics": {
      "status": "training started"
    }
  },
  "tolerance": {
    "final_metrics": {"allow_extra": true}
  },
  "tags": ["demo", "streaming"]
}
