[project]
name = "stock-monte-carlo"
version = "0.1.0"
description = "Geometric Brownian motion stock price Monte Carlo."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "python-dotenv>=1.0",
    "numpy>=1.26",
]

[project.optional-dependencies]
dev = ["pytest>=8", "httpx>=0.27"]
