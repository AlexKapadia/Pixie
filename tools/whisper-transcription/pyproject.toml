[project]
name = "whisper-transcription"
version = "0.1.0"
description = "faster-whisper transcription with streaming segments."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "python-dotenv>=1.0",
  "faster-whisper>=1.0",
  "numpy>=1.26",
]
