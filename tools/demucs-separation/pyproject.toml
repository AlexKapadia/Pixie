[project]
name = "demucs-separation"
version = "0.1.0"
description = "Audio source separation via Demucs with a graceful fallback."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "python-dotenv>=1.0",
  "numpy>=1.26",
  "scipy>=1.13",
  "soundfile>=0.12",
]
