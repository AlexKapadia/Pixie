[project]
name = "cellular-automata"
version = "0.1.0"
description = "Cellular automata explorer (Conway's Life + Wolfram 1-D rules)."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "python-dotenv>=1.0",
  "numpy>=1.26",
  "pillow>=10.0",
]
