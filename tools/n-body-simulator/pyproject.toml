[project]
name = "n-body-simulator"
version = "0.1.0"
description = "Symplectic N-body gravitational simulator."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "python-dotenv>=1.0",
  "numpy>=1.26",
]
