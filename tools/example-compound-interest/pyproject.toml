[project]
name = "example-compound-interest"
version = "0.1.0"
description = "Canonical example tool for Pixie: compound interest calculator."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "python-dotenv>=1.0",
]
