[project]
name = "llm-tool-use-agent"
version = "0.1.0"
description = "Streaming LLM tool-use agent (calculator + web-search stub)."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "python-dotenv>=1.0",
  "anthropic>=0.40",
]
