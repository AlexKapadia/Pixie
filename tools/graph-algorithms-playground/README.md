# Graph Algorithms Playground

Run Dijkstra shortest path, maximum flow, or greedy-modularity community detection on a small weighted graph supplied as `a-b:weight` text. Visualises the result on a network plot.

## Install
```
cd tools/graph-algorithms-playground
uv sync
```

## Test
```
uv run pixie validate graph-algorithms-playground --summary
```
