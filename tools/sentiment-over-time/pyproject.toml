[project]
name = "sentiment-over-time"
version = "0.1.0"
description = "Per-row VADER sentiment with rolling-window aggregation."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "python-dotenv>=1.0",
  "pandas>=2.2",
  "vaderSentiment>=3.3.2",
]
