# System prompt for {{TOOL_NAME}}

Replace this file with your tool's system instructions. The default
implementation in `src/{{PACKAGE}}/streaming.py` ignores it (it just
echoes), so swap that out at the same time you change this prompt.
